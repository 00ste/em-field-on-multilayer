#include <cmath>
#include <iostream>

#include "../Multilayer.h"

int main(int argc, char* argv[]) {
    double angleOfIncidence = M_PI / 6;
    double E1inc = 1;

    Multilayer f = Multilayer({ 10 }, { 1 });
    f.

    return EXIT_SUCCESS;
}
